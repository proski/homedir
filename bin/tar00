#! /bin/sh

# Tar wrapper that removes user and group ID from the created archive

: ${TAR=gtar}

if test $# -lt 2; then
  exec $TAR "$@"
fi

arg1="$1"
shift
arg2="$1"
shift

exec $TAR "$arg1" "$arg2" --owner=0 --group=0 "$@"
